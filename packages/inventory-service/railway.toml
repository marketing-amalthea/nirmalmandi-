[build]
builder = "NIXPACKS"
buildCommand = "cd /app && npm install --workspaces --if-present && npm run build --workspace=packages/inventory-service"

[deploy]
startCommand = "node packages/inventory-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
