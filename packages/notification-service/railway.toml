[build]
builder = "NIXPACKS"
buildCommand = "cd /app && npm install --workspaces --if-present && npm run build --workspace=packages/notification-service"

[deploy]
startCommand = "node packages/notification-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
