[build]
builder = "NIXPACKS"
buildCommand = "cd /app && npm install --workspaces --if-present && npm run build --workspace=packages/logistics-service"

[deploy]
startCommand = "node packages/logistics-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
