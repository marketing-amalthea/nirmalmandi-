[build]
builder = "NIXPACKS"
buildCommand = "cd /app && npm install --workspaces --if-present && npm run build --workspace=packages/auth-service"

[deploy]
startCommand = "node packages/auth-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
