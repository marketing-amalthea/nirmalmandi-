[build]
builder = "NIXPACKS"
buildCommand = "cd /app && npm install --workspaces --if-present && npm run build --workspace=packages/order-service"

[deploy]
startCommand = "node packages/order-service/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
